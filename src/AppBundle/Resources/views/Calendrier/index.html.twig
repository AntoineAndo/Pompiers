{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Calendrier list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Pompier</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('calendrier_show', { 'slug': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }} {{ entity.prenom }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('calendrier_show', { 'slug': entity.slug }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('calendrier_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
