{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Calendrier creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('calendrier') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
